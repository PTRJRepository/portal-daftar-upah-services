{
  "dynamic_headers_by_gang_month_optimized": {
    "sql": "SELECT DISTINCT t.DocDesc FROM PR_ADTRANS_ARC AS t JOIN PR_ADTRANSLN_ARC AS ln ON t.ID = ln.MasterID JOIN HR_GANGLN AS g ON g.GangMember = t.EmpCode WHERE g.GangCode = ? AND t.DocDate >= ? AND t.DocDate < ? AND COALESCE(ln.Amount,0) > 0 AND t.DocDesc IS NOT NULL AND UPPER(t.DocDesc) LIKE 'PREMI%' ORDER BY t.DocDesc"
  },
  "dynamic_headers_by_gang_month_with_amounts": {
    "sql": "SELECT t.DocDesc, SUM(COALESCE(ln.Amount,0)) AS TotalAmount FROM PR_ADTRANS_ARC AS t JOIN PR_ADTRANSLN_ARC AS ln ON t.ID = ln.MasterID JOIN HR_GANGLN AS g ON g.GangMember = t.EmpCode WHERE g.GangCode = ? AND t.DocDate >= ? AND t.DocDate < ? AND UPPER(t.DocDesc) LIKE 'PREMI%' GROUP BY t.DocDesc HAVING SUM(COALESCE(ln.Amount,0)) > 0 ORDER BY TotalAmount DESC"
  },
  "dynamic_headers_by_gang_month": {
    "sql": "SELECT t.DocDesc, SUM(COALESCE(ln.Amount,0)) AS TotalAmount FROM PR_ADTRANS_ARC AS t JOIN PR_ADTRANSLN_ARC AS ln ON t.ID = ln.MasterID JOIN HR_GANGLN AS g ON g.GangMember = t.EmpCode WHERE g.GangCode = ? AND t.DocDate >= ? AND t.DocDate < ? AND COALESCE(ln.Amount,0) > 0 AND t.DocDesc IS NOT NULL AND UPPER(t.DocDesc) LIKE 'PREMI%' GROUP BY t.DocDesc HAVING SUM(COALESCE(ln.Amount,0)) > 0 ORDER BY SUM(COALESCE(ln.Amount,0)) DESC"
  },
  "dynamic_premi_headers_filtered": {
    "sql": "SELECT DISTINCT t.DocDesc FROM \"PR_ADTRANS_ARC\" AS t JOIN \"PR_ADTRANSLN_ARC\" AS ln ON t.ID = ln.MasterID WHERE t.EmpCode IN (SELECT \"HR_EMPLOYEE\".\"EmpCode\" FROM \"HR_EMPLOYEE\" JOIN \"HR_GANGLN\" ON \"HR_GANGLN\".\"GangMember\" = \"HR_EMPLOYEE\".\"EmpCode\" WHERE \"HR_GANGLN\".\"GangCode\" = ?) AND t.DocDate >= ? AND t.DocDate < ? AND UPPER(t.DocDesc) LIKE 'PREMI%' AND t.DocDesc IS NOT NULL ORDER BY t.DocDesc"
  },
  "dynamic_premi_with_amounts": {
    "sql": "SELECT DISTINCT t.DocDesc, SUM(COALESCE(ln.Amount,0)) as total_amount FROM \"PR_ADTRANS_ARC\" AS t JOIN \"PR_ADTRANSLN_ARC\" AS ln ON t.ID = ln.MasterID WHERE t.EmpCode IN (SELECT \"HR_EMPLOYEE\".\"EmpCode\" FROM \"HR_EMPLOYEE\" JOIN \"HR_GANGLN\" ON \"HR_GANGLN\".\"GangMember\" = \"HR_EMPLOYEE\".\"EmpCode\" WHERE \"HR_GANGLN\".\"GangCode\" = ?) AND t.DocDate >= ? AND t.DocDate < ? AND UPPER(t.DocDesc) LIKE 'PREMI%' AND t.DocDesc IS NOT NULL GROUP BY t.DocDesc ORDER BY t.DocDesc"
  }
}